[package]
name = "hpm5301_pac"
version = "0.1.0"
edition = "2024"

[lib]
test = false
bench = false

[dependencies]
critical-section = { version = "1.2.0", optional = true }
vcell = "0.1.3"

[features]
critical-section = ["dep:critical-section"]
rt = []
